{
    "version": 2,
    "builds": [{ "src": "index.js", "use": "@vercel/node" }],
    "routes": [{ "src": "/(.*)", "dest": "/index.js" }],
    "scripts": {
        "build": "npm install && npx puppeteer@21.10.0 browsers install"
      }
  }
  